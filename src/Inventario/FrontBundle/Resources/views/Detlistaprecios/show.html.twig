{% extends "InventarioFrontBundle:Default:index.html.twig" %}

{% block sitecontent -%}


    <h1>Detlistaprecios</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dbvalor</th>
                <td>{{ entity.dbvalor }}</td>
            </tr>
            <tr>
                <th>Tximagen</th>
                <td>{{ entity.tximagen }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('detlistaprecios') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <a href="{{ path('detlistaprecios_edit', { 'id': entity.id }) }}">
            Modificar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
