{% extends '::base.html.twig' %}

{% block sitecontent %}
    <h1>Detdocumentos edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('detdocumentos') }}">
            Volver al listado
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
