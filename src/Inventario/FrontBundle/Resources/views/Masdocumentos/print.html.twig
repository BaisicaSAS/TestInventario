{% extends "InventarioFrontBundle:Default:index.html.twig" %}

{% block sitecontent -%}
    <table id="doc_print">
        {% for entity in entities %}
            <tr>
                <td>{{ entity.incantidad }}</td>
                <td>{{ entity.txrefinterna }}</td>
                <td>{{ entity.txnomproducto }}</td>
                <td>{{ entity.dbvalunitario }}</td>
                <td>{{ entity.dbvaltotal }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
